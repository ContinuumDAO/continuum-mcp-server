/**
 * Saved chain definitions: RPC URLs, gas defaults, block explorers, name/symbol, etc.
 */

export async function loadNetworksRegistry(): Promise<void> {
  // Reserved for network / chain registry.
}
